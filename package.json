{
  "name": "mysolution",
  "version": "1.0.0",
  "description": "ALC 7 Days code challenge",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel public -d lib",
    "start": " babel public -d lib && babel-node main.js",
    "dev": "nodemon main.js --exec babel-node"
  },
  "keywords": [
    "Javascript",
    "IndexDB",
    "Cache",
    "API",
    "Service Worker"
  ],
  "author": "sulenchy",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "nodemon": "^1.17.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
